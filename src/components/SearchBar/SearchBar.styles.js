import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  search: {
    backgroundColor: '#ECEFF1',
    padding: 10,
    margin: 5,
    height: 45,
    borderRadius: 10,
  },
});
